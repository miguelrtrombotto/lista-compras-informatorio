# Archivos compilados / optimizados / DLL
__pycache__/
*.py[codz]
*$py.class

# Extensiones C
*.so

# Distribución / empaquetado
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usualmente estos archivos los escribe un script de Python desde una plantilla
#  antes de que PyInstaller construya el exe, para inyectar fecha/otra info en él.
*.manifest
*.spec

# Registros del instalador
pip-log.txt
pip-delete-this-directory.txt

# Reportes de tests unitarios / cobertura
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Traducciones
*.mo
*.pot

# Cosas de Django:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Cosas de Flask:
instance/
.webassets-cache

# Cosas de Scrapy:
.scrapy

# Documentación Sphinx
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   Para una librería o paquete, quizá quieras ignorar estos archivos ya que el código
#   está pensado para correr en múltiples entornos; de lo contrario, inclúyelos en control:
# .python-version

# pipenv
#   Según pypa/pipenv#598, se recomienda incluir Pipfile.lock en control de versiones.
#   Sin embargo, en colaboración, si hay dependencias específicas por plataforma o sin soporte
#   multiplataforma, pipenv puede instalar dependencias que no funcionen, o no instalar todas.
#Pipfile.lock

# UV
#   Similar a Pipfile.lock, generalmente se recomienda incluir uv.lock en control de versiones.
#   Especialmente recomendado para paquetes binarios para garantizar reproducibilidad; más
#   comúnmente ignorado en librerías.
#uv.lock

# poetry
#   Similar a Pipfile.lock, generalmente se recomienda incluir poetry.lock en control de versiones.
#   Especialmente recomendado para paquetes binarios para garantizar reproducibilidad; más
#   comúnmente ignorado en librerías.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock
#poetry.toml

# pdm
#   Similar a Pipfile.lock, generalmente se recomienda incluir pdm.lock en control de versiones.
#   pdm recomienda incluir la configuración del proyecto en pdm.toml, pero excluir .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
#pdm.lock
#pdm.toml
.pdm-python
.pdm-build/

# pixi
#   Similar a Pipfile.lock, generalmente se recomienda incluir pixi.lock en control de versiones.
#pixi.lock
#   Pixi crea un entorno virtual en el directorio .pixi, igual que el módulo venv lo crea
#   en el directorio .venv. Se recomienda no incluir este directorio en control de versiones.
.pixi

# PEP 582; usado por ej. github.com/David-OConnor/pyflow y github.com/pdm-project/pdm
__pypackages__/

# Cosas de Celery
celerybeat-schedule
celerybeat.pid

# Archivos parseados de SageMath
*.sage.py

# Entornos
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Configuraciones de proyecto de Spyder
.spyderproject
.spyproject

# Documentación mkdocs
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Checker de tipos Pyre
.pyre/

# Analizador estático pytype
.pytype/

# Símbolos de depuración de Cython
cython_debug/

# PyCharm
#  La plantilla específica de JetBrains se mantiene en un JetBrains.gitignore separado que
#  se encuentra en https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  y puede añadirse al gitignore global o fusionarse en este archivo. Para una opción más drástica
#  (no recomendada) puedes descomentar lo siguiente para ignorar toda la carpeta .idea.
#.idea/

# Abstra
# Abstra es un framework de automatización de procesos con IA.
# Ignorar directorios con credenciales de usuario, estado local y configuraciones.
# Más info en https://abstra.io/docs
.abstra/

# Visual Studio Code
#  La plantilla específica de VS Code se mantiene en un VisualStudioCode.gitignore separado
#  que está en https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#  y puede añadirse al gitignore global o fusionarse en este archivo. Sin embargo, si preferís,
#  podés descomentar lo siguiente para ignorar toda la carpeta .vscode
# .vscode/

# Cosas de Ruff:
.ruff_cache/

# Archivo de configuración de PyPI
.pypirc

# Cursor
#  Cursor es un editor de código con IA. `.cursorignore` especifica archivos/directorios a
#  excluir de funciones de IA como autocompletado y análisis. Recomendado para datos sensibles
#  ver https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# Entorno virtual (forma clara como carpeta)
.venv/

# VS Code (ignorar configuraciones locales del editor)
.vscode/

# Datos locales (archivos de trabajo)
data/*.json
# Si querés mantener un ejemplo en el repo, descomenta la siguiente línea:
# !data/ejemplo.json

# Archivos de sistema (Windows / macOS)
Thumbs.db
desktop.ini
.DS_Store

# Logs genéricos
*.log